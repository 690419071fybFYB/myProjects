# 启动命令手册（hioshop）

适用目录：
- 服务端（API）：`/Volumes/SAMSUNG/fyb/myProjects/hioshop-server`
- 管理后台（Web）：`/Volumes/SAMSUNG/fyb/myProjects/hioshop-admin-web`
- 小程序端：`/Volumes/SAMSUNG/fyb/myProjects/hioshop-miniprogram`

## 0. 开发环境速启（推荐顺序）

1. 启动 MySQL（确保本机 `127.0.0.1:3306` 可用）
2. 启动服务端 API（8360）
3. 启动管理后台 Web（9529）
4. 小程序端用微信开发者工具打开并运行

---

## 1. 服务端（hioshop-server）

### 1.1 安装依赖

```bash
cd /Volumes/SAMSUNG/fyb/myProjects/hioshop-server
npm install
```

### 1.2 启动（开发模式）

```bash
cd /Volumes/SAMSUNG/fyb/myProjects/hioshop-server
npm start
```

启动成功后访问：
- 服务端主页/健康检查：`http://127.0.0.1:8360/`
- 管理端登录接口：`POST http://127.0.0.1:8360/admin/auth/login`
- 小程序 API 前缀：`http://127.0.0.1:8360/api/`

### 1.3 初始化数据库（首次必做）

1) 创建数据库（utf8mb4）：

```sql
CREATE SCHEMA `hiolabsDB` DEFAULT CHARACTER SET utf8mb4;
```

2) 导入 SQL（项目根目录自带 `hiolabsDB.sql.zip`）：

```bash
cd /Volumes/SAMSUNG/fyb/myProjects/hioshop-server
unzip -o hiolabsDB.sql.zip
mysql -uroot hiolabsDB < hiolabsDB.sql
```

如果你的 root 有密码：

```bash
mysql -uroot -p hiolabsDB < hiolabsDB.sql
```

### 1.4 关键配置文件位置

- 数据库配置：[database.js](file:///Volumes/SAMSUNG/fyb/myProjects/hioshop-server/src/common/config/database.js)
- 小程序登录/支付等配置：[config.js](file:///Volumes/SAMSUNG/fyb/myProjects/hioshop-server/src/common/config/config.js)

### 1.5 常用校验命令

检查服务是否在监听 8360：

```bash
lsof -i :8360 -sTCP:LISTEN -n -P
```

快速验证管理员登录接口（默认账号）：

```bash
curl -s -X POST http://127.0.0.1:8360/admin/auth/login \
  -H 'Content-Type: application/x-www-form-urlencoded' \
  --data 'username=qilelab.com&password=qilelab.com'
```

### 1.6 常见问题（服务端）

- MySQL 8/9 认证协议导致 `ER_NOT_SUPPORTED_AUTH_MODE`：
  - 当前本项目已通过使用 mysql2 兼容解决；如你重新安装依赖后又出现该错误，检查 `node_modules/think-mysql/index.js` 首行是否为 `require('mysql2')`，并确认 `mysql2` 已安装。

---

## 2. 管理后台（hioshop-admin-web）

### 2.1 安装依赖

```bash
cd /Volumes/SAMSUNG/fyb/myProjects/hioshop-admin-web
npm install
```

### 2.2 启动（开发模式）

```bash
cd /Volumes/SAMSUNG/fyb/myProjects/hioshop-admin-web
npm run dev
```

启动成功后访问：
- `http://localhost:9529`

### 2.3 打包构建

```bash
cd /Volumes/SAMSUNG/fyb/myProjects/hioshop-admin-web
npm run build:prod
```

### 2.4 管理后台请求的服务端地址

管理后台 API 根地址配置在：
- [api.js](file:///Volumes/SAMSUNG/fyb/myProjects/hioshop-admin-web/src/config/api.js#L1-L3)

默认是：
- `http://127.0.0.1:8360/admin/`

### 2.5 常见问题（管理端）

- Node 17+ 启动 vue-cli/webpack4 报 `ERR_OSSL_EVP_UNSUPPORTED`：
  - 当前项目已在脚本中加入 `NODE_OPTIONS=--openssl-legacy-provider`，见 [package.json](file:///Volumes/SAMSUNG/fyb/myProjects/hioshop-admin-web/package.json#L6-L15) 的 `dev`。

---

## 3. 小程序端（hioshop-miniprogram）

### 3.1 启动方式

小程序端不通过 npm 启动，使用「微信开发者工具」打开目录并运行：

```text
/Volumes/SAMSUNG/fyb/myProjects/hioshop-miniprogram
```

### 3.2 配置 API 地址

小程序 API 根地址在：
- [api.js](file:///Volumes/SAMSUNG/fyb/myProjects/hioshop-miniprogram/config/api.js#L1-L8)

开发者工具本地调试可使用：
- `http://127.0.0.1:8360`

真机调试必须改为：
- 局域网 IP（同一网络下能访问到你的电脑），或
- 已备案且配置到小程序后台白名单的 HTTPS 域名

---

## 4. 停止服务/重启的常用命令

### 4.1 前台方式启动（推荐）
- 终端前台运行的进程：直接 `Ctrl + C` 停止

### 4.2 端口被占用时排查

```bash
lsof -i :8360 -sTCP:LISTEN -n -P
lsof -i :9529 -sTCP:LISTEN -n -P
```

拿到 PID 后：

```bash
kill <PID>
```

